{
  "name": "hub-02-tax",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Tax and paycheck calculator hub. Hub 2 of the utility site portfolio.",
  "scripts": {
    "dev": "astro dev --port 3026",
    "build": "npm run data:check && astro build",
    "preview": "astro preview --port 3026",
    "astro": "astro",
    "data:check": "node scripts/check-data.mjs",
    "data:report": "node scripts/check-data.mjs --report",
    "test": "node --import ./scripts/alias-loader.mjs scripts/test-tax.mjs"
  },
  "dependencies": {
    "astro": "^5.6.1",
    "@fontsource-variable/inter": "^5.2.5"
  }
}
